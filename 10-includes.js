const pets = ["cat", "dog", "bat"]; 

const res1 = pets.includes("cat"); // includes a diferencia de los otros metodos no recibe una arrow function, simplemente hay que escribir dentro del parentesis el elemento que queremos saber si está. 
console.log(res1);

